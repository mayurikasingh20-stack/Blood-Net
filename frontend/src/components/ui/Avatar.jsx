/** User image with initials fallback. */
export default function Avatar({ src, name = "User", size = "md", className = "" }) { const initials = name.split(" ").filter(Boolean).slice(0, 2).map((part) => part[0]).join("").toUpperCase(); const sizes = { sm: "h-8 w-8 text-xs", md: "h-10 w-10 text-sm", lg: "h-14 w-14 text-lg" }; return src ? <img src={src} alt={name} className={`rounded-full object-cover ${sizes[size]} ${className}`} /> : <span aria-label={name} className={`inline-flex items-center justify-center rounded-full bg-red/10 font-bold text-red-deep ${sizes[size]} ${className}`}>{initials}</span>; }
