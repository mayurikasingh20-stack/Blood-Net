/** File picker with optional drag-and-drop affordance; consumers handle uploads. */
export default function FileUpload({ label, id, hint = "Choose a file", accept, multiple = false, onChange, className = "" }) { const inputId = id || "file-upload"; return <label htmlFor={inputId} className={`block ${className}`}><span className="mb-1.5 block text-sm font-semibold">{label}</span><span className="flex cursor-pointer flex-col items-center rounded-lg border border-dashed border-ink/30 bg-paper px-4 py-6 text-center text-sm transition hover:border-red hover:bg-red/5"><strong className="text-red-deep">Browse files</strong><span className="mt-1 text-ink-soft">{hint}</span></span><input id={inputId} type="file" accept={accept} multiple={multiple} onChange={onChange} className="sr-only" /></label>; }
