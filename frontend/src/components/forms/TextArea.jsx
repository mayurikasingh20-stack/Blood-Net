/** Multi-line version of Input with the same label/error contract. */
export default function TextArea({ label, error, hint, id, className = "", required, rows = 4, ...props }) { const inputId = id || props.name; return <label className={`block ${className}`} htmlFor={inputId}><span className="mb-1.5 block text-sm font-semibold">{label}{required && <span className="ml-1 text-red">*</span>}</span><textarea id={inputId} required={required} rows={rows} aria-invalid={Boolean(error)} className="w-full resize-y rounded-lg border border-ink/20 bg-paper px-3 py-2.5 text-sm transition placeholder:text-ink-soft/60 hover:border-ink/40 focus:border-red focus:outline-none" {...props}/>{error ? <span className="mt-1 block text-xs text-red-700">{error}</span> : hint && <span className="mt-1 block text-xs text-ink-soft">{hint}</span>}</label>; }
