/** Labelled single-line field, including validation text. */
export default function Input({ label, error, hint, id, className = "", required, ...props }) { const inputId = id || props.name; return <label className={`block ${className}`} htmlFor={inputId}><span className="mb-1.5 block text-sm font-semibold text-ink">{label}{required && <span className="ml-1 text-red" aria-hidden="true">*</span>}</span><input id={inputId} required={required} aria-invalid={Boolean(error)} aria-describedby={error ? `${inputId}-error` : undefined} className="w-full rounded-lg border border-ink/20 bg-paper px-3 py-2.5 text-sm transition placeholder:text-ink-soft/60 hover:border-ink/40 focus:border-red focus:outline-none" {...props}/>{error ? <span id={`${inputId}-error`} className="mt-1 block text-xs text-red-700">{error}</span> : hint && <span className="mt-1 block text-xs text-ink-soft">{hint}</span>}</label>; }
