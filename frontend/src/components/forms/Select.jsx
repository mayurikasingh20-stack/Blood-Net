/** Labelled select using an options array of { value, label, disabled }. */
export default function Select({ label, options = [], placeholder = "Select an option", error, id, className = "", required, ...props }) { const inputId = id || props.name; return <label className={`block ${className}`} htmlFor={inputId}><span className="mb-1.5 block text-sm font-semibold">{label}{required && <span className="ml-1 text-red">*</span>}</span><select id={inputId} required={required} aria-invalid={Boolean(error)} className="w-full rounded-lg border border-ink/20 bg-paper px-3 py-2.5 text-sm transition hover:border-ink/40 focus:border-red focus:outline-none" {...props}><option value="">{placeholder}</option>{options.map((option) => <option key={option.value} value={option.value} disabled={option.disabled}>{option.label}</option>)}</select>{error && <span className="mt-1 block text-xs text-red-700">{error}</span>}</label>; }
