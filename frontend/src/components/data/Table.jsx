import EmptyState from "../feedback/EmptyState";
/** Generic table driven by columns ({ key, header, render, className }) and rows. */
export default function Table({ columns = [], rows = [], getRowKey = (row, index) => row.id ?? index, emptyTitle, emptyDescription, className = "" }) { if (!rows.length) return <EmptyState title={emptyTitle || "No records found"} description={emptyDescription} />; return <div className={`overflow-x-auto rounded-xl border border-ink/10 bg-paper ${className}`}><table className="w-full min-w-[640px] text-left text-sm"><thead className="bg-ink/[.03] text-xs uppercase tracking-wide text-ink-soft"><tr>{columns.map((column) => <th key={column.key} scope="col" className={`px-4 py-3 font-bold ${column.className || ""}`}>{column.header}</th>)}</tr></thead><tbody className="divide-y divide-ink/10">{rows.map((row, index) => <tr key={getRowKey(row, index)} className="transition hover:bg-red/[.025]">{columns.map((column) => <td key={column.key} className={`px-4 py-3 ${column.className || ""}`}>{column.render ? column.render(row, index) : row[column.key]}</td>)}</tr>)}</tbody></table></div>; }
