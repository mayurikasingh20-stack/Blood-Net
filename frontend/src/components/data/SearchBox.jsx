/** Controlled search input. Debouncing remains the responsibility of its parent. */
export default function SearchBox({ value, onChange, placeholder = "Search", label = "Search", className = "" }) { return <label className={`relative block ${className}`}><span className="sr-only">{label}</span><span aria-hidden="true" className="absolute left-3 top-2.5 text-ink-soft">⌕</span><input value={value} onChange={(event) => onChange(event.target.value)} placeholder={placeholder} className="w-full rounded-lg border border-ink/20 bg-paper py-2 pl-9 pr-3 text-sm focus:border-red focus:outline-none" /></label>; }
