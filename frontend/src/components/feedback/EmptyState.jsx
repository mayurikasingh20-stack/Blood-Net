import Button from "../ui/Button";
export default function EmptyState({ title = "Nothing here yet", description, actionLabel, onAction, icon = "○", className = "" }) { return <section className={`rounded-xl border border-dashed border-slate-200 bg-white px-6 py-10 text-center ${className}`}><div aria-hidden="true" className="mb-3 text-3xl text-red">{icon}</div><h3 className="font-bold text-xl font-bold">{title}</h3>{description && <p className="mx-auto mt-2 max-w-md text-sm text-slate-500">{description}</p>}{actionLabel && <Button className="mt-5" onClick={onAction}>{actionLabel}</Button>}</section>; }
