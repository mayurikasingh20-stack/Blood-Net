/** Static contextual message. ErrorMessage and SuccessMessage are convenience wrappers. */
export default function Alert({ children, tone = "info", title, className = "", role = "status" }) { const tones = { info: "border-blue-200 bg-blue-50 text-blue-950", success: "border-green/30 bg-green/10 text-green", error: "border-red/30 bg-red/10 text-red-900", warning: "border-amber/40 bg-amber/10 text-amber-900" }; return <div role={role} className={`rounded-lg border px-4 py-3 text-sm ${tones[tone]} ${className}`}>{title && <p className="mb-1 font-bold">{title}</p>}{children}</div>; }
