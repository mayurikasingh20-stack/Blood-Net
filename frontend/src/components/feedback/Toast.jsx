import { useEffect } from "react";
/** Controlled, dismissible notification; render from a shared app-level toast host later. */
export default function Toast({ message, tone = "success", onDismiss, duration = 4000 }) { useEffect(() => { if (!onDismiss || !duration) return undefined; const timer = setTimeout(onDismiss, duration); return () => clearTimeout(timer); }, [duration, onDismiss]); if (!message) return null; const tones = { success: "bg-green", error: "bg-red-800", info: "bg-ink" }; return <div role="status" className={`fixed bottom-5 right-5 z-[70] flex max-w-sm items-center gap-4 rounded-lg px-4 py-3 text-sm font-semibold text-white shadow-lg ${tones[tone]}`}><span>{message}</span>{onDismiss && <button type="button" onClick={onDismiss} aria-label="Dismiss notification" className="opacity-80 hover:opacity-100">×</button>}</div>; }
